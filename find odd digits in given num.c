#include <stdio.h>
#include<math.h>
int main()
{int a,b,n,num,rem;
printf("enter the number");
scanf("%d",&num);
while(num>n)
{a=num%10;
num=num/10;
rem=a%2;
if(rem!=0)
printf("%d",a);
   
}return 0;
}
