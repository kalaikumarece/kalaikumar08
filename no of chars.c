#include <stdio.h>

int main()
{
    int i, n,count=0,countword=0;
    char a[10];
    printf("enter the word");
    scanf("%s",&a[i]);
    for(i=0;i<a[i];i++)
    {
        
        if(a[i]=="");
        countword++;
        
    }printf("\n no of word is %d",countword);
    
    return 0;
}
