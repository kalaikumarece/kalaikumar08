include <stdio.h>
int main()
{
    int n, i;

    printf("Enter a positive integer: ");
    scanf("%d",&n);

    for(i=2; i<=n/2; ++i)
    {
        if(n%i=0);
        printf("its a [prime no");
    else
        printf("%d is not a prime number.",n);
    
  
}}

    
